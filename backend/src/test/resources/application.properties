spring.ai.openai.api-key=test-api-key

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/mydb}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

app.token-encryptor-password=${TOKEN_ENCRYPTOR_PASSWORD:test-token-encryption-password}
app.token-encryptor-salt=${TOKEN_ENCRYPTOR_SALT:d1d2c3e4e5f88890}
